{% extends 'base.html.twig' %}

{% block title %}Hello ContactsController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    {% if contacts  %}
        <table>
            <thead>
                <tr>
                    <td>
                        Name
                    </td>
                    <td>
                        last name
                    </td>
                    <td>
                        Actions
                    </td>
                </tr>
            </thead>
            <tbody>
                {% for contact in contacts %}
                    <tr>
                        <td>
                            {{ contact.Firstname }}
                        </td>
                        <td>
                            {{ contact.LastName }}
                        </td>
                        <td>
                            <a href="{{ path('view', {"id": contact.id}) }}">View</a>
                            <a href="{{ path('update', {"id": contact.id}) }}">Update</a>
                            <a href="{{ path('delete', {"id": contact.id}) }}">Delete</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <h2>
            No contacts added.
        </h2>
    {% endif %}

    <h4>
        <a href="{{ path('create') }}">
            Add a new one
        </a>
    </h4>
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/fabioconceicao/Documents/projects/LILLYDOO/src/Controller/ContactsController.php'|file_link(0) }}">src/Controller/ContactsController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/fabioconceicao/Documents/projects/LILLYDOO/templates/contacts/index.html.twig'|file_link(0) }}">templates/contacts/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
